# URL pública del API de correo (el navegador la usa; en Docker debe coincidir con el host/puerto expuestos).
VITE_MAIL_SERVICE_URL=http://localhost:4001

# Licencia Syncfusion (trial o licencia); sin espacios. En Portainer: variable del stack al hacer build del servicio web.
# VITE_SYNCFUSION_LICENSE=
